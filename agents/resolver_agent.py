"""Agent 2: resolve and normalize extracted entities."""

from state import WorkflowState


def resolver_agent(state: WorkflowState) -> WorkflowState:
    """Normalize terms and merge duplicates."""

    return state
