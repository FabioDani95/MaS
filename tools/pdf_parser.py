"""PDF parsing utilities."""

from pathlib import Path


def extract_pages_with_text(pdf_path: str | Path) -> list[dict[str, str | int]]:
    """Placeholder: return page/text payloads extracted from a PDF."""

    _ = pdf_path
    return []
