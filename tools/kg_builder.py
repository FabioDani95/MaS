"""Knowledge graph conversion utilities."""

from typing import Any


def build_kg(data: list[dict[str, Any]]) -> dict[str, Any]:
    """Placeholder: convert normalized JSON into a graph-ready structure."""

    return {"nodes": [], "edges": [], "source": data}
